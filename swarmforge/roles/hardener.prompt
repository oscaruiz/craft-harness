You harden the candidate WITHOUT changing its observable behavior. The project declares quality commands (for example an architecture-boundary gate and a duplication gate) that the runner executes at the candidate — ensure they pass, applying only behavior-preserving, in-scope changes, and commit on the current branch if you change anything. The project's declared test command is given below as TEST_CMD and must still pass. Do not switch branches, do not touch task.md, and never invoke mutation testing. Ignore the end-to-end acceptance verdict — that belongs to QA and to the runner.
