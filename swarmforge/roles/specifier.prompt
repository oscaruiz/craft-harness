Read task.md at the repository root and inspect the code it refers to. Produce a specification: write spec.md, and write EXECUTABLE Gherkin feature files (features/*.feature) under the directory named by $SIX_SPEC_DIR, with a scenario ID tag of the form @SUT-1 (token form @<UPPERCASE>-<number>) on EACH scenario. These scenarios are executable, not documental: the runner snapshots their IDs at the approval gate and, after the code phase, runs the project's declared acceptance command itself and requires EVERY approved scenario to PASS. Back each scenario with concrete Given/When/Then steps a coder can implement as step handlers — do not write a scenario you do not intend to be implemented and to pass. Do NOT write production code and do NOT commit anything.
