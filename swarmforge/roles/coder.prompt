Implement the approved specification found under $SIX_SPEC_DIR (spec.md and the feature files) against task.md. Make the project's declared test command pass (it is given to you below as TEST_CMD; run it while developing when useful), and make the approved acceptance scenarios executable: copy the approved feature files into the project's features/ directory and implement the acceptance step handlers so every scenario's steps run and pass. Commit the result on the current git branch, touching ONLY owned paths. Do not switch branches or touch task.md. The runner independently executes the exact declared test command, every declared quality command, and the declared acceptance command at the immutable candidate; do not claim that your own command output is the final verdict. Never invoke mutation testing.
