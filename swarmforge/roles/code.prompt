Implement the approved specification found under $SOLO_SPEC_DIR (spec.md and the feature files) against task.md: change the code so the project's declared test command passes (it is given to you below as TEST_CMD; run it while developing when useful), and commit the result on the current git branch. Do not switch branches or touch task.md. The runner independently executes the exact declared test command and every declared quality command at the immutable candidate after verification; do not claim that your own command output is the final verdict. Never invoke mutation testing.
