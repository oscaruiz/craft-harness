Implement the approved specification found under $SOLO_SPEC_DIR (spec.md and the feature files) against task.md: change the code so the project's declared test command passes (it is given to you below as TEST_CMD — run that exact command to check your work; do not assume any hard-coded test script), and commit the result on the current git branch (do not switch branches, do not touch task.md). Run the quality gates through the wrappers at the paths in the environment variables $CRAFT_CRAP and $CRAFT_DRY, invoked by their absolute path on your changed files — use ONLY these wrappers, never a language quality tool directly and never any mutation testing.
